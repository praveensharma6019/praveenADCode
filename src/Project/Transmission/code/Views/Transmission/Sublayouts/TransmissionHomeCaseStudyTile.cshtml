@using Sitecore.Mvc.Presentation
@using Sitecore.Foundation.SitecoreExtensions.Extensions
@using Sitecore.Mvc.Presentation
@using Sitecore
@using Sitecore.Foundation.SitecoreExtensions.Extensions
@using Sitecore.Foundation.Dictionary.Repositories
@using Sitecore.Foundation.Dictionary.Extensions
@using System.Text.RegularExpressions
@using Sitecore.Foundation.SitecoreExtensions.Extensions
@using Sitecore.Data
@using Sitecore.Foundation.SitecoreExtensions.Extensions
@using Sitecore.Resources.Media;
@using Sitecore.Data.Items;
@using Sitecore
@using Sitecore.Data.Fields

@model RenderingModel
@{
    var datasource = Sitecore.Mvc.Presentation.RenderingContext.Current.Rendering.DataSource;
    var elements = Sitecore.Context.Database.GetItem(datasource);
    var techInn = elements.Children;
	 var sortedparent = techInn.OrderByDescending(x => x.Fields["Date"].Value);
    var itemURL = "#";
	var i=0;
}


<div class="owl-carousel owl-theme case-study h-100">

    @foreach (var item in techInn.ToList())
    {
	 Sitecore.Data.Fields.LinkField linkvalues = item.Fields["LinkUrl"];
                                 itemURL = linkvalues.GetFriendlyUrl();
	if(i<3)
	{
       // itemURL = Sitecore.Links.LinkManager.GetItemUrl(item);
<div class="item h-100">
    <div class="blog-block h-100 p-4">
        <a href="@itemURL" target="_blank">
            <div class="bg-images">
                @Html.Sitecore().ImageField("Image", item, 0, 0, "img-fluid")
            </div>
            <div class="h-100">
                <h3 class="section-heading text-white text-left">Tech & Innovation</h3>
                <p class="px-4">@Html.Sitecore().Field("Title", item)</p>
            </div>
        </a>
    </div>
</div>
}
i++;
}
</div>
