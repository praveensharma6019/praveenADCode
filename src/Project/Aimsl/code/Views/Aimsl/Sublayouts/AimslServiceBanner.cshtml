
@using Sitecore.Data.Fields
@using Sitecore.Feature.Media
@using Sitecore.Feature.Media.Repositories
@using Sitecore.Foundation.Alerts
@using Sitecore.Foundation.Alerts.Extensions
@using Sitecore.Foundation.Dictionary.Extensions
@using Sitecore.Foundation.SitecoreExtensions.Extensions
@using Sitecore.Foundation.Theming.Extensions
@using Sitecore.Resources.Media
@model Sitecore.Mvc.Presentation.RenderingModel
@{
    /**/

    /**/

    //var datasource = Sitecore.Mvc.Presentation.RenderingContext.Current.Rendering.DataSource;
    //var elements = Sitecore.Context.Database.GetItem(datasource);
    //var Children =  elements.Children;
    //var ContentPath = Sitecore.Context.Item.Paths.ContentPath.Replace("/Ports/", "");
    //var path = ContentPath.Replace("/Ports/", "");
    //string[] pathArr = path.Split('/');
    //var navPath = "/";
    //var pageName = "";

    var datasource = Sitecore.Mvc.Presentation.RenderingContext.Current.Rendering.DataSource;
    var elements = Sitecore.Context.Database.GetItem(datasource);
    var Children = elements.Children;
    var ContentPath = Sitecore.Context.Item.Paths.ContentPath.Replace("/Aimsl/", "");
    var path = ContentPath.Replace("/Aimsl/", "");
    string[] pathArr = path.Split('/');
    var navPath = "/";
    var i = 0;
    var pageName = "";
}


@*<div class="breadcrumb-banner">
        <div class="bg-images"> @Html.Sitecore().ImageField("MediaThumbnail", elements, 0, 0, "image-fluid")</div>
        <div class="container position-relative d-block">
            <ul>
                @for (i = 0; i < pathArr.Count(); i++)
                {
                    navPath = navPath + pathArr[i];
                    pageName = pathArr[i].Replace("-", " ").Substring(0, 1).ToUpper() + "" + pathArr[i].Replace("-", " ").Substring(1).ToLower();

                    <li><a href="@navPath">@pageName</a></li>
                    navPath = navPath + "/";
                }
            </ul>
            @Html.Sitecore().Field("MediaDescription", elements)
        </div>
    </div>*@

<section class="breadcrumb-banner">
    <div class="bg-images">
        @Html.Sitecore().ImageField("MediaImage", elements, 0, 0, "image-fluid")
    </div>
    <div class="container position-relative d-block">
        <ul>
            @*@for (int i = 0; i < pathArr.Count(); i++)
        {
            navPath = navPath + pathArr[i];
            pageName = pathArr[i].Replace("-", " ").Substring(0, 1).ToUpper() + "" + pathArr[i].Replace("-", " ").Substring(1).ToLower();

            <li><a href="@navPath">@pageName</a></li>
            navPath = navPath + "/";
        }*@
            
                @for (i = 0; i < pathArr.Count() - 1; i++)
                {
                    navPath = navPath + pathArr[i];
                    pageName = pathArr[i].Replace("-", " ").Substring(0, 1).ToUpper() + "" + pathArr[i].Replace("-", " ").Substring(1).ToLower();

                    <li><a href="@navPath">@pageName</a></li>
                    navPath = navPath + "/";
                }
            
        </ul>
        @Html.Sitecore().Field("MediaDescription", elements)
    </div>
</section>