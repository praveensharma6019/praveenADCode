@using Sitecore.Foundation.SitecoreExtensions.Extensions
@using Sitecore.Data.Fields
@using Sitecore.Data.Items

@{
    var datasource = Sitecore.Mvc.Presentation.RenderingContext.Current.Rendering.DataSource;
    var element = Sitecore.Context.Database.GetItem(datasource);


}

<section class="bg-white section_padding-lg section_padding-bottom">
    <div class="container-fluid">
        <h2 class="heading">@Html.Sitecore().Field("Title", element)</h2>
        <div class="owl-carousel campus-carousel">
            @foreach (Item items in element.GetChildren())
            {
                Sitecore.Data.Fields.ImageField imgField = ((Sitecore.Data.Fields.ImageField)items.Fields["MediaImage"]);

                string imgurl = Sitecore.Resources.Media.MediaManager.GetMediaUrl(imgField.MediaItem);
        <div class="item"><img src="@imgurl"></div>
            }
        </div>
        <div class="campus-carousel-counter"></div>
    </div>
</section>