@using Sitecore.Feature.Navigation
@using Sitecore.Foundation.SitecoreExtensions.Extensions
@using Sitecore.Foundation.Dictionary.Extensions
@model Sitecore.Feature.Navigation.Models.NavigationItems

@{
    if (Model?.NavItems == null)
    {
        return;
    }
}
<ul class="social-ul">
    @foreach (var item in Model.NavItems)
    {
        if (item.IsNewTab)
        {
            <li>
                <a target="_blank" href="@item.Url" target="@item.Target"><span class="@item.Item[Templates.LinkMenuItem.Fields.Icon]"></span></a>
            </li>
        }
        else
        {
            <li>
                <a href="@item.Url" target="@item.Target"><span class="@item.Item[Templates.LinkMenuItem.Fields.Icon]"></span></a>
            </li>
        }
    }
</ul>
