@{
    Layout = null;
}

@using Adani.SuperApp.Airport.Feature.Navigation.Platform.Models
@model Adani.SuperApp.Airport.Feature.Navigation.Platform.Models.HeaderMyAccountLogin

@{
    if (Model == null)
    {
        return;
    }
}

<ul class="hamburger_section hamburger-header single-item">
    <li>
        <a href="@Model.Link">
            <i class="@Model.LeftIcon"></i>
            <label>@Html.Raw(Model.Title) </label>
            <span class="@Model.RightIcon"></span>
        </a>
    </li>
</ul>

