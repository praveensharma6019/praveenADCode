@using Sitecore.Foundation.Dictionary.Extensions
@using Sitecore.Foundation.SitecoreExtensions.Extensions
@using Sitecore.Mvc.Configuration
@using Sitecore
@using Sitecore.Mvc
@model Sitecore.Mvc.Presentation.RenderingModel

@{
    // var id = Sitecore.Context.Database.GetItem("about-us").Url();
    string RedirectUrl = Request.Url.Scheme + "://" + Request.Url.Host;
    var pagename = Sitecore.Links.LinkManager.GetItemUrl(Sitecore.Context.Item);
}
<script src="https://www.google.com/recaptcha/api.js?onload=myCallBack&render=explicit" async defer></script>

<div class="modal video" id="AdaniGasSubscribeUsModal" style="padding-right: 17px;">
    <div class="modal-dialog modal-dialog-centered modal-xs">
        <div class="modal-content modal-lg">
            <button type="button" class="close" data-dismiss="modal">×</button>
            <!-- Modal body -->
            <div class="modal-body bg-white c-tracking p-4">
                <h3 class="mb-3 txt-md">Subscribe to our latest updates</h3>
                <div class="subscribe-form">
                    <form id="subscribe-form" name="subscribe-form">
                        <input type="hidden" id="sFormType" value="Subscribe-Form">
                        <input type="hidden" id="PageInfo" value="">
                        <input type="hidden" id="utm_source" value="">
                        <input type="hidden" id="emailMessage" value="{EE41E08A-8DBC-4319-8922-F212CEBA728D}">
                        <div class="form-group">
                            <input type="email" name="subscribeEmail" id="subscribeEmail" class="form-control" placeholder="Email">
                        </div>
                        <div class="col-md-4 mb-3">
                            <div id="recaptcha2"></div>
                        </div>
                        <div class="form-group text-right">
                            <input type="submit" value="Subscribe" id="btnSubscribeUsSubmit" class="btn btn-gradient">
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>